@model IEnumerable<Blog.Domain.Entities.CategoryWithPostsNumber>

@{
    var categoriesCode = new[] { "angularjs", "javascript", "cordova", "asp-net-mvc", "html5" };
}

<ul>
    @foreach (var item in Model)
    {
        if (categoriesCode.Contains(item.Code))
        {
            var title = item.PostsNumber > 1 ? "Voir les " + item.PostsNumber + " articles" : "Voir l'article";

            <li>
                <a href="@Url.RouteUrl("PostsListForCategory", new { categoryCode = item.Code })" title="@title">
                    @item.Name
                    <span class="count">@item.PostsNumber</span>
                </a>
            </li>
        }
    }
</ul>